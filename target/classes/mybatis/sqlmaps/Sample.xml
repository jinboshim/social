<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Sample">
	
	<!-- 관리자 권한 리스트 조회 -->
    <select id="test" parameterType="EData" resultType="EData">
		SELECT *
		FROM TB_USER
		WHERE USER_NO = 1
    </select>
    

</mapper>